# ====================================
# PUERTOS LOCALES PARA DOCKER-COMPOSE
# ====================================
# Copia este archivo como .env y ajusta los puertos según tu estación de trabajo
# El archivo .env NO se sube a git (está en .gitignore)

# Puerto del backend (mapeo host:container)
BACKEND_PORT=3001

# Puerto del frontend (mapeo host:container)
FRONTEND_PORT=5173

# Puerto de MySQL (mapeo host:container)
MYSQL_PORT=3307

# ====================================
# NOTAS
# ====================================
# - Si algún puerto está ocupado en tu máquina, cambia el número en .env
# - Ejemplo: Si 3001 está ocupado, usa BACKEND_PORT=3002
# - Los puertos internos del container (después de :) NO se modifican
# - Valores por defecto: BACKEND=3001, FRONTEND=5173, MYSQL=3307
